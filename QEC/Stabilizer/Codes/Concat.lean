import QEC.Stabilizer.Codes.Concat.SteaneSteane

/-!
# Concatenated codes

Concrete instances of the CSS concatenation framework
(`Framework/Concatenation`, milestones M1–M6).

- `Codes.Concat.SteaneSteane` (M7) — Steane ⊗ Steane: `steaneConcatData : ConcatCSSData 7 7 1`,
  the input bundle for the `[[49, 1, ≥ 9]]` concatenated code.
-/
