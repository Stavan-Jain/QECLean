/.lake
.gauss/
.DS_Store

# Cursor local state and continual-learning index
.cursor/

# Claude Code local state
.claude/*

# Learned preferences/facts (optional: keep local only)
AGENTS.md

# Personal toolchain-upgrade runbook (not part of published docs)
TOOLCHAIN_UPGRADE.md

# Local maintenance scripts (kept local)
scripts/prune-stale-ileans.sh

project_aristotle

# Local-only reference PDFs (not part of the formalization)
*.pdf

# Blueprint build output (`leanblueprint web` / `leanblueprint pdf`).
# The sources live in blueprint/src/; the extracted LaTeX is generated into
# .lake/build/blueprint by `lake build QECBlueprint:blueprint`.
/blueprint/web/
/blueprint/print/
/blueprint/src/web.paux
/blueprint/src/*.bbl
